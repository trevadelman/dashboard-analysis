# routes — FastAPI APIRouter modules for the trading dashboard.
#
# Each module exposes a create_router(bot, login_required) factory that
# returns a configured APIRouter.  app.py imports and registers them all.
